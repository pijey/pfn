import sampleController from '../../../utils/sample-controller';
import { module, test } from 'qunit';

module('Unit | Utility | sample controller');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sampleController();
  assert.ok(result);
});
