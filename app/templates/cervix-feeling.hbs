<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h2 class="text-center">Col {{model.id}}</h2>
    <form class="form-horizontal" role="form" {{action 'ok' on='submit' target=view}}>
      <div class="form-group">
        <label for="inputCycleDayNumber" class="col-sm-3 control-label">Jour du cycle</label>
        <div class="input-group col-sm-6">
          {{input type="text" class="form-control" id="inputCycleDayNumber" value=model.cycle_day_number}}
        </div>
        <label for="inputDate" class="col-sm-3 control-label">Date</label>
        <div class="input-group col-sm-6">
          {{bs-datetimepicker date=model.date format='DD/MM/YYYY'}}
          {{!-- {{input type="text" id="inputDate" value=model.date}} --}}
        </div>
        <label for="inputSensation" class="col-sm-3 control-label">Sensation</label>
        <div class="input-group col-sm-6">
          {{input type="text" class="form-control" id="inputSensation" value=model.sensation}}
        </div>
        <label for="inputOpening" class="col-sm-3 control-label">Ouverture</label>
        <div class="input-group col-sm-6">
          {{input type="text" class="form-control" id="inputOpening" value=model.opening}}
        </div>
        <label for="inputPosition" class="col-sm-3 control-label">Position</label>
        <div class="input-group col-sm-6">
          {{input type="text" class="form-control" id="inputPosition" value=model.position}}
        </div>
        <label for="inputInclining" class="col-sm-3 control-label">Inclinaison</label>
        <div class="input-group col-sm-6">
          {{input type="text" class="form-control" id="inputInclining" value=model.inclining}}
        </div>
        <label for="inputComment" class="col-sm-3 control-label">Commentaire</label>
        <div class="input-group col-sm-6">
          {{input type="text" class="form-control" id="inputComment" value=model.comment}}
        </div>
      </div>
      <div class="form-group">
        <div class="col-sm-offset-3">
          <button {{action "save" model}} class="btn btn-default">
            {{#if model.isSaving}}
              <img src="assets/images/loader.gif" alt="">
            {{else}}
              {{#if model.isDirty}}
                <span class="glyphicon glyphicon-floppy-save"></span>
              {{else}}
                <span class="glyphicon glyphicon-floppy-saved"></span>
              {{/if}}
            {{/if}}
            &nbsp;Enregistrer
          </button>
          <button {{action "cancel" model}} class="btn btn-default">
            <span class="glyphicon glyphicon-remove"></span>
            &nbsp;Annuler
          </button>
        </div>
      </div>
    </form>
  </div>
</div>